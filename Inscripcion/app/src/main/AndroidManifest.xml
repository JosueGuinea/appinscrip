<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sv.edu.ucad.inscripcion" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo175x175px"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".SplashActivity"
            android:label="@string/BarraSplash"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".PpalActivity"
            android:label="Iniciar Sesión"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".NuevoUsuario"
            android:label="@string/BarraNewUser" >
        </activity>

        <activity
            android:name=".layout_inicio_inscripcion"
            android:label="@string/title_activity_layout_inicio_inscripcion"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>
    </application>

</manifest>
